@{
    ViewData["Title"] = "Dashboard";
    ViewData["ActiveNav"] = "Dashboard";
    dynamic m = Model;
}
<div class="row g-3">
    <div class="col-md-3">
        <div class="card p-3">
            <div class="text-muted small">Total Users</div>
            <div class="display-6">@m.TotalUsers</div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card p-3">
            <div class="text-muted small">Teachers</div>
            <div class="display-6">@m.TotalTeachers</div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card p-3">
            <div class="text-muted small">Students</div>
            <div class="display-6">@m.TotalStudents</div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card p-3">
            <div class="text-muted small">Pending Approvals</div>
            <div class="display-6">@m.PendingUsers</div>
        </div>
    </div>

    <div class="col-md-6">
        <div class="card p-3">
            <div class="text-muted small">Active Workspaces</div>
            <div class="display-6">@m.ActiveWorkspaces</div>
        </div>
    </div>
    <div class="col-md-6">
        <div class="card p-3">
            <div class="text-muted small">Inactive Workspaces</div>
            <div class="display-6">@m.InactiveWorkspaces</div>
        </div>
    </div>
</div>

<div class="mt-4 d-flex gap-2">
    <a class="btn btn-outline-primary" href="@Url.Action("Workspaces", "Admin")">Manage Workspaces</a>
    <a class="btn btn-outline-secondary" href="@Url.Action("Users", "Admin")">Manage Users</a>
</div>
